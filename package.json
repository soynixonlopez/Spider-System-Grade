{
  "name": "spider-grades-system",
  "version": "1.0.0",
  "description": "Sistema de gestión de calificaciones para estudiantes",
  "main": "index.js",
  "scripts": {
    "setup": "node setup.js",
    "dev": "live-server public",
    "build:css": "tailwindcss -i ./src/css/input.css -o ./public/css/style.css --watch",
    "build": "tailwindcss -i ./src/css/input.css -o ./public/css/style.css --minify",
    "start": "npm run build && live-server public"
  },
  "keywords": ["grades", "education", "firebase", "tailwind"],
  "author": "IT Teacher",
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "firebase": "^10.7.0"
  }
}
